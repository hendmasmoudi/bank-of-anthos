# ==========================================================
# filename: rbac-admin-breakglass.yaml
# RBAC: Break-Glass Administrator
# Scope: Cluster-wide
# Purpose: Exceptional use only (setup, recovery, emergency tasks)
# Principle: Not used for routine operations
# ==========================================================

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: boa-admin-breakglass-binding
subjects:
  - kind: User
    name: boa-admin
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io